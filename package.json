{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Portfolio site for a learning designer, built with Eleventy.",
  "scripts": {
    "start": "eleventy --serve",
    "dev": "eleventy --serve",
    "build": "eleventy",
    "clean": "rm -rf _site",
    "build-ghpages": "npx @11ty/eleventy --pathprefix=/portfolio/"
  },
  "license": "MIT",
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}